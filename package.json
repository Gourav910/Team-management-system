{
  "name": "team-management-system",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "build": "cd client && npm install && npm run build && cd ../server && npm install && npx prisma generate && npm run build",
    "start": "cd server && npm run db:push && npm start"
  }
}
